<%= form_with(model: strength) do |form| %>
  <% if strength.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(strength.errors.count, "error") %> prohibited this strength from being saved:</h2>

      <ul>
        <% strength.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :domain, style: "display: block" %>
    <%= form.text_field :domain %>
  </div>

  <div>
    <%= form.submit class: "button" %>
  </div>
<% end %>
